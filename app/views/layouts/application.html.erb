<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<title>Stories of Health » <%= @subtitle %></title>
	<meta name="google-site-verification" content="XekN5oqG6ufti1e2OxMZLWkL-kbO4ujduRTcviSMzic" />
	<meta name="keywords" content="healthcare, health, care, broken, insurance, reform, story, stories, video, cancer, diabetes, medicare, medical, debt, public option, unemployed" />
	<meta name="description" content="<%= meta_tag_description %>" />
	<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
	<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>

	<% if Rails.env.production? || ["index"].index(params[:action]) %>
		<script type="text/javascript">
		<% if Rails.env.production? %>
			$.trackPage("UA-1895263-5");
			$.fn.track.defaults.debug = false;
		<% end %>
		<% if params[:action] == "index" %>
			<% if Rails.env.production? %>
				var videoId = '<%= random_trailer_id %>';
			<% else %>
				var videoId = "u1zgFlCw8Aw"; // youtube demo
			<% end %>
		<% end %>
	</script>
	<% end %>
</head>

<body>
	<div class="container">
		<div class="narrow">
			<%= link_to(image_tag("logo-small.png", :size=>"312x212", :alt=>"Stories of Health"), :action => :index) %>

			<div id="about">
			<p>Health care in the United States is broken.</p>

			<p>To fully understand how severe of a crisis we&rsquo;re in, people from all over California told us about their experiences with a system in dire need of reform.</p>
			<% if params[:action] != "action" %>
			<p><input class="button" type="button" value="Take Action &#9658;" onclick="window.location='<%= url_for(:action => "action") %>'" /></p>

			<p class="action-line">Call your member of congress <em>today</em>:</p>
			<p class="action"> <strong>877-264-4226</strong></p>
			<% end %>
			</div>
		</div>

		<div class="wide">
		<%= yield %>
		</div><!-- wide -->

		<div id="footer">
			<ul class="site">
				<li><%= link_to("About", :action => :about) %></li>
				<li><%= link_to("Share Your Story", :action => :share) %></li>
				<li><%= link_to("Take Action", :action => "action") %></li>
				<li><%= link_to("All Stories", :action => "stories") %></li>
				<li><%= link_to("Trailers", :action => :trailers) %></li>
			</ul>
			<ul class="links">
				<li><a href="https://twitter.com/storiesofhealth">Follow us on Twitter</a></li>
				<li><a href="https://www.facebook.com/storiesofhealth">Become a fan on Facebook</a></li>
			</ul>
		</div>
	</div>

</body>
</html>
